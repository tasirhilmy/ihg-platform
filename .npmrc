# Allow lifecycle scripts (postinstall) to run — needed for @prisma/engines binary download
# Faster, more reliable installs
prefer-offline=true
audit=false

# Use legacy peer deps for compatibility
legacy-peer-deps=true
